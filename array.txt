//Array Input/Output

#include <stdio.h>
void main() {
	int a[10],i;
	for (i=0;i<10;i++):
		printf("Enter the %d number for the array:",i);
		scanf("%d",&a[i]);
	printf("array values are: \n");
	for (i=0;i<10;i++):
		printf("%d \n",a[i]);
}

